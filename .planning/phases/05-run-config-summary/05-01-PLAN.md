---
phase: 05-run-config-summary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mlpstorage_py/storage_config.py
  - mlpstorage_py/benchmarks/dlio.py
  - mlpstorage_py/checkpointing/storage_readers/minio_reader.py
  - mlpstorage_py/checkpointing/storage_readers/s3torch_reader.py
  - mlpstorage_py/checkpointing/storage_writers/minio_writer.py
  - mlpstorage_py/checkpointing/storage_writers/s3dlio_writer.py
  - mlpstorage_py/checkpointing/storage_writers/s3torch_writer.py
  - tests/unit/test_storage_config.py
autonomous: true
requirements:
  - RUNSUM-04
  - RUNSUM-05
  - RUNSUM-06
  - RUNSUM-07

must_haves:
  truths:
    - "resolve_object_storage_config() returns a dict with exactly these keys: bucket, storage_library, uri_scheme, endpoint, load_balance_strategy, aws_region, aws_ca_bundle, aws_access_key_id_redacted, aws_secret_access_key_redacted"
    - "endpoint value is a (value, source_label) tuple; source_label is the env var name that provided the value"
    - "AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY are never present as raw strings in the returned dict — only redacted forms appear"
    - "All 6 storage files (dlio.py, minio_reader.py, s3torch_reader.py, minio_writer.py, s3dlio_writer.py, s3torch_writer.py) import from storage_config instead of calling os.environ.get() directly for the catalogued S3 vars"
    - "The os.environ write at s3dlio_writer.py line 173 is untouched"
    - "pytest tests/unit -v shows no new failures vs. baseline (1001 pass, 30 pre-existing fail)"
  artifacts:
    - path: "mlpstorage_py/storage_config.py"
      provides: "resolve_object_storage_config() — single source of S3 env var reads"
      exports: ["resolve_object_storage_config"]
    - path: "tests/unit/test_storage_config.py"
      provides: "Unit tests for resolver (RUNSUM-04, RUNSUM-05, RUNSUM-06)"
      contains: "TestCredentialRedaction, TestEndpointResolution, TestCentralizedResolver"
  key_links:
    - from: "mlpstorage_py/benchmarks/dlio.py"
      to: "mlpstorage_py/storage_config.py"
      via: "from mlpstorage_py.storage_config import resolve_object_storage_config"
      pattern: "resolve_object_storage_config"
    - from: "mlpstorage_py/checkpointing/storage_writers/s3dlio_writer.py"
      to: "mlpstorage_py/storage_config.py"
      via: "resolve_object_storage_config"
      pattern: "os.environ\\['AWS_ENDPOINT_URL'\\] = selected"
---

<objective>
Create the centralized S3 environment-variable resolver (`storage_config.py`) and replace all scattered
`os.environ.get()` calls for S3 configuration in the six existing storage files with imports from it.

Purpose: RUNSUM-06 requires a single source of truth for all S3 env var reads. This plan is a
pure refactor — no behavior changes from the user's perspective. It also establishes the `(value,
source_label)` endpoint tuple needed by Plan 05-02's summary display.

Output:
- `mlpstorage_py/storage_config.py` — new module with `resolve_object_storage_config()`
- `tests/unit/test_storage_config.py` — new test file covering RUNSUM-04, RUNSUM-05, RUNSUM-06
- 6 modified storage files — scattered reads replaced with centralized imports
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key patterns extracted from existing source files for executor reference. -->

From mlpstorage_py/benchmarks/dlio.py lines 180-187 (current scattered reads to replace):
  bucket = os.environ.get('BUCKET', '')
  storage_library = os.environ.get('STORAGE_LIBRARY', 's3dlio')
  endpoint_url = os.environ.get('AWS_ENDPOINT_URL', '')
  uri_scheme = os.environ.get('STORAGE_URI_SCHEME', 's3').rstrip(':/')

From mlpstorage_py/checkpointing/storage_readers/minio_reader.py:
  _detect_endpoint() static method at lines 30-55 reads: S3_ENDPOINT_URIS, S3_ENDPOINT_TEMPLATE,
  S3_ENDPOINT_FILE, then falls through to AWS_ENDPOINT_URL / S3_ENDPOINT.
  __init__ also reads: AWS_ACCESS_KEY_ID (line 75), AWS_SECRET_ACCESS_KEY (line 76),
  AWS_CA_BUNDLE (line 96), AWS_REGION (line 114).
  Lines 53+61: OMPI_COMM_WORLD_RANK, PMI_RANK — NOT moved.

From mlpstorage_py/checkpointing/storage_readers/s3torch_reader.py:
  _detect_endpoint() at lines 52-73 reads: S3_ENDPOINT_URIS, S3_ENDPOINT_TEMPLATE, S3_ENDPOINT_FILE,
  AWS_ENDPOINT_URL, S3_ENDPOINT.
  __init__ also reads: AWS_REGION (line 96).

From mlpstorage_py/checkpointing/storage_writers/minio_writer.py:
  _detect_and_select_endpoint() at lines 88-135 reads: S3_ENDPOINT_URIS, S3_ENDPOINT_TEMPLATE,
  S3_ENDPOINT_FILE, AWS_ENDPOINT_URL, S3_ENDPOINT.
  __init__ also reads: AWS_ACCESS_KEY_ID (line 186), AWS_SECRET_ACCESS_KEY (line 187),
  AWS_CA_BUNDLE (line 221), AWS_REGION (line 239).
  Lines 53+61: OMPI_COMM_WORLD_RANK, PMI_RANK — NOT moved.

From mlpstorage_py/checkpointing/storage_writers/s3dlio_writer.py:
  _detect_multi_endpoint_config() at lines 127-175 reads: S3_ENDPOINT_URIS, S3_ENDPOINT_TEMPLATE,
  S3_ENDPOINT_FILE.
  Line 173: os.environ['AWS_ENDPOINT_URL'] = selected — THIS WRITE MUST NOT BE REMOVED.
  Lines 234, 259: S3_LOAD_BALANCE_STRATEGY reads.

From mlpstorage_py/checkpointing/storage_writers/s3torch_writer.py:
  _detect_and_select_endpoint() at lines 83-129 reads: S3_ENDPOINT_URIS, S3_ENDPOINT_TEMPLATE,
  S3_ENDPOINT_FILE, AWS_ENDPOINT_URL, S3_ENDPOINT.
  __init__ also reads: AWS_REGION (line 170).
  Lines 47+55: OMPI_COMM_WORLD_RANK, PMI_RANK — NOT moved.

From tests/unit/test_dlio_object_storage.py (test pattern to replicate):
  import os
  from argparse import Namespace
  from unittest.mock import MagicMock, patch, call
  import pytest
  monkeypatch.setenv('BUCKET', bucket)
  monkeypatch.delenv('STORAGE_LIBRARY', raising=False)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create storage_config.py and test_storage_config.py (Wave 0 tests first)</name>
  <files>
    mlpstorage_py/storage_config.py
    tests/unit/test_storage_config.py
  </files>
  <read_first>
    - mlpstorage_py/benchmarks/dlio.py (lines 1-10 for import style; lines 175-200 for the 4 env reads being centralized)
    - mlpstorage_py/checkpointing/storage_readers/minio_reader.py (lines 25-60 for _detect_endpoint fallback chain; lines 70-120 for __init__ reads)
    - tests/unit/test_dlio_object_storage.py (lines 1-30 and 160-200 for monkeypatch env pattern)
    - tests/unit/test_main_warnings.py (lines 1-30 for logger patch pattern)
  </read_first>
  <behavior>
    - TestCredentialRedaction.test_access_key_redacted_when_set: set AWS_ACCESS_KEY_ID='AKIAIOSFODNN7EXAMPLE', call resolve_object_storage_config(), assert 'AKIAIOSFODNN7EXAMPLE' not in str(config) and '[SET —' in config['aws_access_key_id_redacted']
    - TestCredentialRedaction.test_access_key_not_set: delenv AWS_ACCESS_KEY_ID, call resolver, assert config['aws_access_key_id_redacted'] == '[not set]'
    - TestCredentialRedaction.test_secret_key_redacted: same pattern for AWS_SECRET_ACCESS_KEY / aws_secret_access_key_redacted
    - TestEndpointResolution.test_s3_endpoint_uris_takes_priority: set both S3_ENDPOINT_URIS and AWS_ENDPOINT_URL, assert val == S3_ENDPOINT_URIS value and src == 'S3_ENDPOINT_URIS'
    - TestEndpointResolution.test_aws_endpoint_url_fallback: unset the four higher-priority vars, set AWS_ENDPOINT_URL, assert src == 'AWS_ENDPOINT_URL'
    - TestEndpointResolution.test_no_endpoint_set: delenv all 5 chain vars, assert config['endpoint'] == (None, '')
    - TestEndpointResolution.test_endpoint_returns_tuple: assert isinstance(config['endpoint'], tuple) and len == 2
    - TestCentralizedResolver.test_defaults_returned_when_env_unset: delenv BUCKET, STORAGE_LIBRARY, STORAGE_URI_SCHEME, S3_LOAD_BALANCE_STRATEGY, AWS_REGION; assert bucket=='', storage_library=='s3dlio', uri_scheme=='s3', load_balance_strategy=='round_robin', aws_region=='us-east-1'
  </behavior>
  <action>
    Write tests first (RED). Then implement storage_config.py (GREEN).

    storage_config.py lives at mlpstorage_py/storage_config.py. Its only public export is
    resolve_object_storage_config(). Imports: os, Optional and Tuple from typing.

    The function signature is: resolve_object_storage_config() -> dict

    Two nested helpers (not exported):
    - _redact(val: Optional[str]) -> str: returns f"[SET — {len(val)} chars]" if val is truthy, else "[not set]"
    - _resolve_endpoint() -> Tuple[Optional[str], str]: iterates the priority chain
      ('S3_ENDPOINT_URIS', 'S3_ENDPOINT_TEMPLATE', 'S3_ENDPOINT_FILE', 'AWS_ENDPOINT_URL', 'S3_ENDPOINT'),
      returns (first non-empty value, var_name_that_provided_it) or (None, '') if all unset

    Return dict keys (exact names — callers depend on these):
      bucket, storage_library, uri_scheme, endpoint (tuple), load_balance_strategy,
      aws_region, aws_ca_bundle, aws_access_key_id_redacted, aws_secret_access_key_redacted

    Defaults:
      BUCKET → '' (empty string, not None)
      STORAGE_LIBRARY → 's3dlio'
      STORAGE_URI_SCHEME → 's3', then .rstrip(':/') applied to result
      S3_LOAD_BALANCE_STRATEGY → 'round_robin'
      AWS_REGION → 'us-east-1'
      AWS_CA_BUNDLE → None (no default)

    Add a module docstring noting that resolve_object_storage_config() reads env vars at call time,
    not at import time, so the .env file may not yet be loaded when called from run_summary.py.
  </action>
  <verify>
    <automated>cd /home/curtis/MLPerfStorage/cli-closed-open/storage && pytest tests/unit/test_storage_config.py -v</automated>
  </verify>
  <done>
    All TestCredentialRedaction, TestEndpointResolution, and TestCentralizedResolver tests pass.
    storage_config.py exists at mlpstorage_py/storage_config.py and exports resolve_object_storage_config.
    Raw credential strings are never present anywhere in the returned dict.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace scattered S3 env reads in all 6 storage files</name>
  <files>
    mlpstorage_py/benchmarks/dlio.py
    mlpstorage_py/checkpointing/storage_readers/minio_reader.py
    mlpstorage_py/checkpointing/storage_readers/s3torch_reader.py
    mlpstorage_py/checkpointing/storage_writers/minio_writer.py
    mlpstorage_py/checkpointing/storage_writers/s3dlio_writer.py
    mlpstorage_py/checkpointing/storage_writers/s3torch_writer.py
  </files>
  <read_first>
    - mlpstorage_py/storage_config.py (the new module from Task 1 — read before touching callers)
    - mlpstorage_py/benchmarks/dlio.py (lines 175-200 for _apply_object_storage_params — the 4 reads to replace)
    - mlpstorage_py/checkpointing/storage_readers/minio_reader.py (lines 25-120 — _detect_endpoint + __init__ reads)
    - mlpstorage_py/checkpointing/storage_readers/s3torch_reader.py (lines 50-105 — _detect_endpoint + __init__ reads)
    - mlpstorage_py/checkpointing/storage_writers/minio_writer.py (lines 85-245 — _detect_and_select_endpoint + __init__ reads)
    - mlpstorage_py/checkpointing/storage_writers/s3dlio_writer.py (lines 125-265 — _detect_multi_endpoint_config + S3_LOAD_BALANCE_STRATEGY reads + line 173 write)
    - mlpstorage_py/checkpointing/storage_writers/s3torch_writer.py (lines 80-180 — _detect_and_select_endpoint + __init__ reads)
  </read_first>
  <action>
    For each file, add the import at the top of the file (after existing imports):
      from mlpstorage_py.storage_config import resolve_object_storage_config

    dlio.py — _apply_object_storage_params() lines 180-187:
      Replace the 4 os.environ.get calls (BUCKET, STORAGE_LIBRARY, AWS_ENDPOINT_URL, STORAGE_URI_SCHEME)
      with: config = resolve_object_storage_config(); then extract:
        bucket = config['bucket']
        storage_library = config['storage_library']
        endpoint_url, _src = config['endpoint']
        endpoint_url = endpoint_url or ''  (preserves existing empty-string semantics)
        uri_scheme = config['uri_scheme']
      Keep all downstream code that uses these variables unchanged.

    minio_reader.py — replace _detect_endpoint() body and __init__ reads:
      In __init__, after existing setup, call config = resolve_object_storage_config() once.
      Replace the endpoint reads with: endpoint, _src = config['endpoint']
      Keep: access_key = os.environ.get('AWS_ACCESS_KEY_ID') (raw — SDK auth, not display)
      Keep: secret_key = os.environ.get('AWS_SECRET_ACCESS_KEY') (raw — SDK auth, not display)
      Replace AWS_CA_BUNDLE read with: config['aws_ca_bundle']
      Replace AWS_REGION read with: config['aws_region']
      The _detect_endpoint() static method can be replaced with a one-liner that calls
      resolve_object_storage_config() or removed if no longer called. Do NOT leave dead code
      with the full 3-step chain — remove the old implementation.
      Do NOT touch OMPI_COMM_WORLD_RANK or PMI_RANK reads.

    s3torch_reader.py — same pattern as minio_reader.py:
      Replace _detect_endpoint() body, endpoint reads in __init__ with config['endpoint'] unpack,
      AWS_REGION read with config['aws_region'].
      endpoint = endpoint or None  (preserves None-means-AWS-S3 semantics in this file)
      Do NOT touch OMPI_COMM_WORLD_RANK or PMI_RANK reads.

    minio_writer.py — same pattern but also replaces AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY display reads:
      Note: The ACCESS_KEY and SECRET_KEY reads in __init__ that feed into Minio SDK client
      initialization must remain as direct os.environ.get reads (raw values for auth).
      Replace AWS_CA_BUNDLE, AWS_REGION, and endpoint detection with config values.
      Do NOT touch OMPI_COMM_WORLD_RANK or PMI_RANK reads.

    s3dlio_writer.py — replace _detect_multi_endpoint_config() body and S3_LOAD_BALANCE_STRATEGY reads:
      Call config = resolve_object_storage_config() where the old method was called.
      Unpack: endpoint_val, _src = config['endpoint']
      Replace S3_LOAD_BALANCE_STRATEGY reads at lines 234 and 259 with config['load_balance_strategy'].
      CRITICAL: line 173 os.environ['AWS_ENDPOINT_URL'] = selected — DO NOT REMOVE THIS LINE.
        This write sets the env var for MPI worker subprocesses and must remain unchanged.

    s3torch_writer.py — same pattern as s3torch_reader.py:
      Replace _detect_and_select_endpoint() body.
      Replace AWS_REGION and endpoint reads with config values.
      Do NOT touch OMPI_COMM_WORLD_RANK or PMI_RANK reads.
  </action>
  <acceptance_criteria>
    - grep -rn "os.environ.get('S3_ENDPOINT_URIS'" mlpstorage_py/ returns 0 matches (all moved to resolver)
    - grep -rn "os.environ.get('S3_ENDPOINT_TEMPLATE'" mlpstorage_py/ returns 0 matches
    - grep -rn "os.environ.get('S3_ENDPOINT_FILE'" mlpstorage_py/ returns 0 matches
    - grep -rn "os.environ.get('S3_LOAD_BALANCE_STRATEGY'" mlpstorage_py/ returns 0 matches
    - grep -n "os.environ\['AWS_ENDPOINT_URL'\]" mlpstorage_py/checkpointing/storage_writers/s3dlio_writer.py returns exactly 1 match (the write line — NOT removed)
    - grep -rn "from mlpstorage_py.storage_config import resolve_object_storage_config" mlpstorage_py/ returns exactly 6 matches (one per modified file)
    - pytest tests/unit -v shows no new failures vs. pre-phase baseline (1001 pass, 30 pre-existing fail)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/curtis/MLPerfStorage/cli-closed-open/storage && pytest tests/unit -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 6 files import from storage_config. No S3_ENDPOINT_URIS/TEMPLATE/FILE or S3_LOAD_BALANCE_STRATEGY
    reads remain outside storage_config.py. The os.environ write at s3dlio_writer.py line 173 is intact.
    pytest tests/unit -v shows no new failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → storage_config.py | Env vars are user-controlled strings read via os.environ.get() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | resolve_object_storage_config() return value | mitigate | _redact() helper ensures AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY are never in the returned dict as raw strings — only "[SET — N chars]" or "[not set]" |
| T-05-02 | Tampering | s3dlio_writer.py line 173 env write | accept | Write is intentional (sets AWS_ENDPOINT_URL for MPI subprocess); resolver read path is separate |
</threat_model>

<verification>
After both tasks complete:

1. grep -rn "os.environ.get('S3_ENDPOINT_URIS'" mlpstorage_py/ — expect 0 results
2. grep -rn "os.environ.get('STORAGE_LIBRARY'" mlpstorage_py/benchmarks/ — expect 0 results
3. grep -c "resolve_object_storage_config" mlpstorage_py/storage_config.py — expect >= 1
4. pytest tests/unit/test_storage_config.py -v — all classes pass
5. pytest tests/unit -v — no new failures vs. baseline
</verification>

<success_criteria>
- mlpstorage_py/storage_config.py exists and exports resolve_object_storage_config()
- Endpoint resolution uses the 5-link fallback chain: S3_ENDPOINT_URIS → S3_ENDPOINT_TEMPLATE → S3_ENDPOINT_FILE → AWS_ENDPOINT_URL → S3_ENDPOINT
- Credentials are redacted at the resolver — raw values never returned
- All 6 storage files use the centralized resolver instead of inline os.environ.get() for catalogued S3 vars
- The os.environ write at s3dlio_writer.py (line ~173) is preserved unchanged
- pytest tests/unit -v passes with no new failures
</success_criteria>

<output>
Create .planning/phases/05-run-config-summary/05-01-SUMMARY.md when done.
</output>
