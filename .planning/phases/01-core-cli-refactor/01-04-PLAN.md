---
phase: 01-core-cli-refactor
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - mlpstorage_py/cli/utility_args.py
  - mlpstorage_py/cli/lockfile_args.py
  - mlpstorage_py/cli/__init__.py
autonomous: true
requirements:
  - CLI-04
  - CLI-08

must_haves:
  truths:
    - "add_reports_arguments(parser) has no is_closed parameter"
    - "add_history_arguments(parser) has no is_closed parameter"
    - "add_lockfile_arguments(parser) has no is_closed parameter"
    - "All add_universal_arguments call sites in utility_args.py and lockfile_args.py use the (parser, req_results) signature"
    - "cli/__init__.py exports are consistent with new function signatures"
  artifacts:
    - path: "mlpstorage_py/cli/utility_args.py"
      provides: "Simplified reports and history arg builders"
      contains: "def add_reports_arguments(parser)"
    - path: "mlpstorage_py/cli/lockfile_args.py"
      provides: "Simplified lockfile arg builder"
      contains: "def add_lockfile_arguments(parser)"
    - path: "mlpstorage_py/cli/__init__.py"
      provides: "Updated module exports"
      contains: "add_storage_type_arguments"
  key_links:
    - from: "mlpstorage_py/cli_parser.py"
      to: "mlpstorage_py/cli/__init__.py"
      via: "import add_reports_arguments, add_history_arguments, add_lockfile_arguments"
      pattern: "from mlpstorage_py.cli import"
---

<objective>
Simplify the utility and lockfile arg builder signatures (drop is_closed) and update
cli/__init__.py exports to match the new public API across all arg builders.

Purpose: These are the simplest changes in the phase — both files just pass is_closed through
to add_universal_arguments without ever using it. This plan runs in parallel with plans
01-02 and 01-03.

Output: utility_args.py and lockfile_args.py with no is_closed; __init__.py with correct
exports including add_storage_type_arguments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-cli-refactor/01-PATTERNS.md
@.planning/phases/01-core-cli-refactor/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From mlpstorage_py/cli/common_args.py after Plan 01-01 -->

add_universal_arguments(parser, req_results)   <!-- was (parser, req_fileobj, req_results, offer_object, is_closed) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Simplify utility_args.py and lockfile_args.py signatures</name>
  <files>mlpstorage_py/cli/utility_args.py, mlpstorage_py/cli/lockfile_args.py</files>

  <read_first>
    mlpstorage_py/cli/utility_args.py — read the full file:
      - add_reports_arguments(parser, is_closed) signature and body
      - add_history_arguments(parser, is_closed) signature and body
      - Every add_universal_arguments call site (note the old 5-arg signature being passed)
    mlpstorage_py/cli/lockfile_args.py — read the full file:
      - add_lockfile_arguments(parser, is_closed) signature and body
      - Every add_universal_arguments call site (note the old 5-arg signature being passed)
    .planning/phases/01-core-cli-refactor/01-PATTERNS.md sections "utility_args.py" and "lockfile_args.py"
  </read_first>

  <action>
    UTILITY_ARGS.PY changes:

    Change add_reports_arguments(parser, is_closed) → add_reports_arguments(parser).
    Remove the is_closed parameter from the signature. The body never used is_closed for
    anything other than forwarding it to add_universal_arguments.

    Update every add_universal_arguments call in the body. The old call looked like:
      add_universal_arguments(reportgen, True, True, True, is_closed)
    The new call drops req_fileobj, offer_object, and is_closed:
      add_universal_arguments(reportgen, req_results=True)
    Find all such calls in the file and update them to use the two-parameter signature.

    Change add_history_arguments(parser, is_closed) → add_history_arguments(parser).
    Apply the same call-site update pattern for any add_universal_arguments calls in this function.

    LOCKFILE_ARGS.PY changes:

    Change add_lockfile_arguments(parser, is_closed) → add_lockfile_arguments(parser).
    Update every add_universal_arguments call in the body:
      add_universal_arguments(generate_parser, True, True, True, is_closed)
      → add_universal_arguments(generate_parser, req_results=True)
      add_universal_arguments(verify_parser, True, True, True, is_closed)
      → add_universal_arguments(verify_parser, req_results=True)
    All other argument definitions in both functions are unchanged.

    Do NOT modify any argument definitions other than the function signatures and
    add_universal_arguments call sites.
  </action>

  <verify>
    <automated>python3 -c "
from mlpstorage_py.cli.utility_args import add_reports_arguments, add_history_arguments
from mlpstorage_py.cli.lockfile_args import add_lockfile_arguments
import inspect
for fn, name in [(add_reports_arguments,'add_reports_arguments'),
                 (add_history_arguments,'add_history_arguments'),
                 (add_lockfile_arguments,'add_lockfile_arguments')]:
    params = list(inspect.signature(fn).parameters)
    assert params == ['parser'], f'{name} wrong sig: {params}'
print('All utility signatures correct')
" && grep -c "is_closed" mlpstorage_py/cli/utility_args.py mlpstorage_py/cli/lockfile_args.py | grep -v "^0$" || echo "No is_closed remaining"</automated>
  </verify>

  <acceptance_criteria>
    - `inspect.signature(add_reports_arguments)` parameters are ['parser'] only
    - `inspect.signature(add_history_arguments)` parameters are ['parser'] only
    - `inspect.signature(add_lockfile_arguments)` parameters are ['parser'] only
    - `grep "is_closed" mlpstorage_py/cli/utility_args.py` returns 0 lines
    - `grep "is_closed" mlpstorage_py/cli/lockfile_args.py` returns 0 lines
    - `grep "add_universal_arguments.*True.*True.*True" mlpstorage_py/cli/utility_args.py` returns 0 lines (old 5-arg calls gone)
    - `grep "add_universal_arguments.*True.*True.*True" mlpstorage_py/cli/lockfile_args.py` returns 0 lines
    - `python3 -c "from mlpstorage_py.cli.utility_args import add_reports_arguments, add_history_arguments"` exits 0
    - `python3 -c "from mlpstorage_py.cli.lockfile_args import add_lockfile_arguments"` exits 0
  </acceptance_criteria>

  <done>
    utility_args.py and lockfile_args.py: is_closed removed from all three functions;
    add_universal_arguments calls updated to two-parameter signature
  </done>
</task>

<task type="auto">
  <name>Task 2: Update cli/__init__.py exports</name>
  <files>mlpstorage_py/cli/__init__.py</files>

  <read_first>
    mlpstorage_py/cli/__init__.py — read the full file:
      - Current import statements and __all__ if present
      - Whether add_storage_type_arguments is already exported
      - Whether add_timeseries_arguments is exported
      - Whether validate_* functions are exported
  </read_first>

  <action>
    Update mlpstorage_py/cli/__init__.py to ensure the public API reflects the refactored
    function signatures and new functions.

    The common_args exports block should include add_storage_type_arguments if it is not
    already listed there. The new function was added to common_args.py in Plan 01-01.
    Also ensure add_timeseries_arguments is exported (it may already be; verify).

    The benchmark arg builder exports should still export:
      add_training_arguments, validate_training_arguments
      add_checkpointing_arguments, validate_checkpointing_arguments
      add_vectordb_arguments, validate_vectordb_arguments
      add_kvcache_arguments, validate_kvcache_arguments
    These names have not changed — only their signatures changed.

    The utility and lockfile exports should still export:
      add_reports_arguments, add_history_arguments
      add_lockfile_arguments

    If any import line references a function that was renamed or removed (e.g., if it
    tries to import add_universal_arguments with the old 5-param assumption somewhere in
    __init__.py's logic), fix it.

    Do NOT add any new public names that were not already in __init__.py or are not needed
    by cli_parser.py. The goal is correctness of existing exports, not expansion.

    If __init__.py has no logic and is purely re-exports, verify it imports cleanly after
    Plans 01-01, 01-02, 01-03, and this task are done. If a circular import or import error
    exists, investigate and fix it before declaring done.
  </action>

  <verify>
    <automated>python3 -c "
import mlpstorage_py.cli as cli
required = ['add_universal_arguments', 'add_storage_type_arguments',
            'add_host_arguments', 'add_mpi_arguments', 'add_dlio_arguments',
            'add_reports_arguments', 'add_history_arguments', 'add_lockfile_arguments',
            'add_training_arguments', 'add_checkpointing_arguments',
            'add_vectordb_arguments', 'add_kvcache_arguments']
missing = [name for name in required if not hasattr(cli, name)]
assert not missing, f'Missing exports: {missing}'
print('cli/__init__.py exports OK')
"</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "import mlpstorage_py.cli"` exits 0 with no ImportError
    - `add_storage_type_arguments` is accessible via `mlpstorage_py.cli.add_storage_type_arguments`
    - `add_timeseries_arguments` is accessible via `mlpstorage_py.cli.add_timeseries_arguments`
    - All benchmark arg builders (add_training_arguments, add_checkpointing_arguments,
      add_vectordb_arguments, add_kvcache_arguments) accessible via mlpstorage_py.cli
    - All utility builders (add_reports_arguments, add_history_arguments, add_lockfile_arguments)
      accessible via mlpstorage_py.cli
    - No ImportError or NameError raised on `import mlpstorage_py.cli`
  </acceptance_criteria>

  <done>
    cli/__init__.py imports cleanly; add_storage_type_arguments and add_timeseries_arguments
    exported; all benchmark and utility builders accessible
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API → callers | __init__.py exports define what cli_parser.py can call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | Utility commands not under mode subparsers | accept | Reports/history/lockfile are top-level siblings by design (CLI-08); no benchmark mode gating needed |
| T-01-11 | Denial of Service | Missing export breaks cli_parser.py import | mitigate | Verify all required names accessible before task is done |
</threat_model>

<verification>
After both tasks complete:

1. `python3 -c "from mlpstorage_py.cli import add_reports_arguments, add_history_arguments, add_lockfile_arguments, add_storage_type_arguments"` exits 0
2. `grep "is_closed" mlpstorage_py/cli/utility_args.py mlpstorage_py/cli/lockfile_args.py` returns nothing
3. `python3 -c "import mlpstorage_py.cli"` exits 0
</verification>

<success_criteria>
- add_reports_arguments, add_history_arguments, add_lockfile_arguments all have single parser parameter
- add_universal_arguments called with two-arg signature at all sites in both files
- cli/__init__.py imports cleanly and exports add_storage_type_arguments
</success_criteria>

<output>
Create `.planning/phases/01-core-cli-refactor/01-04-SUMMARY.md` when done
</output>
