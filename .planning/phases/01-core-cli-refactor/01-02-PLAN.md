---
phase: 01-core-cli-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - mlpstorage_py/cli/training_args.py
  - mlpstorage_py/cli/checkpointing_args.py
  - mlpstorage_py/benchmarks/dlio.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - CLI-06

must_haves:
  truths:
    - "add_training_arguments(parser, mode) accepts mode string, not is_closed boolean"
    - "Training model positional is registered BEFORE command subparsers on the same parser"
    - "Training closed branch model choices are MODELS_CLOSED; open uses MODELS_OPEN; whatif uses MODELS"
    - "_add_training_core_args sets defaults for loops, params, allow_invalid_params"
    - "_add_training_open_args adds --loops, --allow-invalid-params, --params, timeseries"
    - "add_checkpointing_arguments(parser, mode) accepts mode string"
    - "checkpointing has a configview subcommand alongside datasize and run"
    - "CheckpointingBenchmark._run() handles args.command == 'configview' without raising"
    - "No is_closed parameter in any training or checkpointing function signature"
  artifacts:
    - path: "mlpstorage_py/cli/training_args.py"
      provides: "Three-tier training arg builder"
      contains: "def add_training_arguments(parser, mode)"
    - path: "mlpstorage_py/cli/checkpointing_args.py"
      provides: "Three-tier checkpointing arg builder with configview"
      contains: "def add_checkpointing_arguments(parser, mode)"
    - path: "mlpstorage_py/benchmarks/dlio.py"
      provides: "configview dispatch + dry_run rename"
      contains: "dry_run"
  key_links:
    - from: "mlpstorage_py/cli/training_args.py"
      to: "mlpstorage_py/config.py"
      via: "import MODELS_CLOSED, MODELS_OPEN, MODELS, ACCELERATORS, ACCELERATORS_CLOSED"
      pattern: "from mlpstorage_py.config import"
    - from: "mlpstorage_py/cli/training_args.py"
      to: "mlpstorage_py/cli/common_args.py"
      via: "import add_universal_arguments with new (parser, req_results) signature"
      pattern: "add_universal_arguments\\(parser"
    - from: "mlpstorage_py/benchmarks/dlio.py"
      to: "mlpstorage_py/cli/checkpointing_args.py"
      via: "CheckpointingBenchmark._run() dispatches on args.command == 'configview'"
      pattern: "args.command"
---

<objective>
Rewrite training_args.py and checkpointing_args.py with the three-tier builder pattern
(core / open / whatif), eliminating is_closed. Add the configview subcommand to
checkpointing. Update benchmarks/dlio.py to handle configview dispatch and rename
args.what_if to args.dry_run.

Purpose: These two arg builders cover the most-used benchmarks. Both files have unresolved
merge conflicts (which dissolve in the rewrite). Both can be done in parallel with Plan 01-03.

Output: training_args.py and checkpointing_args.py with mode-string API; dlio.py with
configview support and dry_run rename.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-cli-refactor/01-RESEARCH.md
@.planning/phases/01-core-cli-refactor/01-PATTERNS.md
@.planning/phases/01-core-cli-refactor/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From mlpstorage_py/cli/common_args.py after Plan 01-01 -->

add_universal_arguments(parser, req_results)
add_host_arguments(parser, required=False)
add_mpi_arguments(parser)
add_dlio_arguments(parser)
add_timeseries_arguments(parser)
add_storage_type_arguments(parser)

<!-- From mlpstorage_py/config.py -->
MODELS_CLOSED = ['unet3d', 'retinanet']
MODELS_OPEN   = ['unet3d', 'retinanet']
MODELS        = ['cosmoflow', 'resnet50', 'unet3d', 'dlrm', 'retinanet', 'flux']
ACCELERATORS_CLOSED = ['b200', 'mi355']   # also used for open mode
ACCELERATORS        = ['h100', 'a100', 'b200', 'mi355']   # whatif only
LLM_MODELS          = [LLAMA3_70B, LLAMA3_405B, LLAMA3_1T, LLAMA3_8B]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite training_args.py with three-tier builder pattern</name>
  <files>mlpstorage_py/cli/training_args.py</files>

  <read_first>
    mlpstorage_py/cli/training_args.py — read the full file to understand current structure:
      - Current add_training_arguments(parser, is_closed) body
      - The conflict block (lines ~143-154)
      - Which arguments are currently mode-gated with if is_closed: / else:
      - Which arguments go on datasize vs datagen vs run vs configview
    .planning/phases/01-core-cli-refactor/01-PATTERNS.md section "training_args.py"
  </read_first>

  <action>
    Rewrite mlpstorage_py/cli/training_args.py. Keep the module docstring.

    UPDATE IMPORTS — add MODELS_OPEN to the config import:
      from mlpstorage_py.config import (
          MODELS, MODELS_CLOSED, MODELS_OPEN, ACCELERATORS, ACCELERATORS_CLOSED,
          DEFAULT_HOSTS, EXEC_TYPE, EXIT_CODE
      )
    Keep all existing imports from common_args (add_storage_type_arguments is already imported).

    REPLACE add_training_arguments(parser, is_closed) with add_training_arguments(parser, mode):
    The function must:
    1. Build model_choices dict: closed→MODELS_CLOSED, open→MODELS_OPEN, whatif→MODELS
    2. Build accel_choices: ACCELERATORS if mode=="whatif" else ACCELERATORS_CLOSED
    3. Call parser.add_argument("model", choices=model_choices, metavar="MODEL",
         help=HELP_MESSAGES['model']) — POSITIONAL, registered BEFORE subparsers
    4. Create training_subparsers = parser.add_subparsers(dest="command", required=True)
    5. Create four subcommand parsers: datasize, datagen, run_benchmark, configview
       (use HELP_MESSAGES keys: 'datasize', 'training_datagen', 'run_benchmark', 'configview')
    6. For each cmd in [("datasize", datasize), ("datagen", datagen),
                        ("run", run_benchmark), ("configview", configview)]:
         _add_training_core_args(cmd_parser, cmd_name, accel_choices)
         if mode in ("open", "whatif"):
             _add_training_open_args(cmd_parser, cmd_name)
         if mode == "whatif":
             _add_training_whatif_args(cmd_parser, cmd_name)

    ADD _add_training_core_args(parser, command, accel_choices):
    - parser.set_defaults(loops=1, params='', allow_invalid_params=False)
    - add_host_arguments(parser)
    - If command != "datasize": parser.add_argument('--client-host-memory-in-gb', '-cm',
        type=float, required=True, help=HELP_MESSAGES['client_host_mem_GB'])
    - parser.add_argument('--num-accelerators', '-na', type=int, required=True,
        help=HELP_MESSAGES['num_accelerators_run'] if command != "datasize"
             else HELP_MESSAGES['num_accelerators_datasize'])
    - parser.add_argument('--accelerator-type', '-at', choices=accel_choices, required=True,
        help=HELP_MESSAGES['accelerator_type'])
    - parser.add_argument('--exec-type', '-et', choices=list(EXEC_TYPE),
        default=EXEC_TYPE.MPI, help="Execution type")
    - add_mpi_arguments(parser)
    - parser.add_argument('--data-dir', '-dd', required=True,
        help="Directory where the dataset is stored")
    - add_dlio_arguments(parser)
    - add_universal_arguments(parser, req_results=(command in ("run", "configview")))
    - If command in ("datagen", "run", "configview"):
        add_storage_type_arguments(parser)
    Note: examine the existing training_args.py carefully to carry forward all existing
    argument definitions (names, short flags, choices, defaults, help strings) — do not
    invent new argument names.

    ADD _add_training_open_args(parser, command):
    - parser.add_argument('--loops', type=int, default=1,
        help="Number of times to run the benchmark")
    - parser.add_argument('--allow-invalid-params', '-aip', action='store_true',
        help="Allow invalid DLIO parameters to be passed")
    - parser.add_argument('--params', '-p', nargs="+", action="append",
        metavar="KEY=VALUE", help=HELP_MESSAGES['params'])
    - If command == "run": add_timeseries_arguments(parser)

    ADD _add_training_whatif_args(parser, command):
    - pass  (no whatif-only training args at this time)

    RESOLVE the conflict block (lines ~143-154): The entire block dissolves because the
    three-tier pattern replaces the old if is_closed: / else: conditional calls.
    The new code calls add_dlio_arguments(parser) (no is_closed) inside _add_training_core_args.
    The conflict markers must not appear in the final file.

    Also keep validate_training_arguments(args) function unchanged (it has no is_closed usage).
  </action>

  <verify>
    <automated>python3 -c "
from mlpstorage_py.cli.training_args import add_training_arguments
import argparse, inspect
sig = inspect.signature(add_training_arguments)
params = list(sig.parameters)
assert params == ['parser', 'mode'], f'Wrong sig: {params}'
# Build a closed branch and verify model positional exists before command subparsers
p = argparse.ArgumentParser()
add_training_arguments(p, 'closed')
print('training_args OK — closed branch built')
" && grep -c "is_closed" mlpstorage_py/cli/training_args.py | grep -q "^0$" && echo "No is_closed found"</automated>
  </verify>

  <acceptance_criteria>
    - `from mlpstorage_py.cli.training_args import add_training_arguments` succeeds
    - `inspect.signature(add_training_arguments)` parameters are ['parser', 'mode']
    - `grep "is_closed" mlpstorage_py/cli/training_args.py` returns 0 lines
    - `grep "<<<<<<\|>>>>>>" mlpstorage_py/cli/training_args.py` returns 0 lines
    - `grep "def _add_training_core_args" mlpstorage_py/cli/training_args.py` returns 1 line
    - `grep "def _add_training_open_args" mlpstorage_py/cli/training_args.py` returns 1 line
    - `grep "def _add_training_whatif_args" mlpstorage_py/cli/training_args.py` returns 1 line
    - Constructing a parser with add_training_arguments(parser, 'closed') does not raise
    - Constructing a parser with add_training_arguments(parser, 'open') does not raise
    - Constructing a parser with add_training_arguments(parser, 'whatif') does not raise
  </acceptance_criteria>

  <done>
    training_args.py rewrites complete; three-tier pattern applied; no is_closed; no conflict markers;
    all three mode strings accepted without error
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite checkpointing_args.py (add configview, mode param) + update dlio.py</name>
  <files>mlpstorage_py/cli/checkpointing_args.py, mlpstorage_py/benchmarks/dlio.py</files>

  <read_first>
    mlpstorage_py/cli/checkpointing_args.py — read the full file:
      - Current add_checkpointing_arguments(parser, is_closed) body
      - The conflict block (lines ~108-121, the add_universal_arguments loop)
      - The --checkpoint-folder argument (HEAD side of conflict — keep it)
      - Existing validate_checkpointing_arguments function
    mlpstorage_py/benchmarks/dlio.py — read the full file:
      - CheckpointingBenchmark._run() method and its command dispatch (if/elif chain or dict)
      - The args.what_if reference(s) (grep for "what_if" in the file)
    .planning/phases/01-core-cli-refactor/01-PATTERNS.md sections "checkpointing_args.py" and "dlio.py"
  </read_first>

  <action>
    PART A — Rewrite mlpstorage_py/cli/checkpointing_args.py:

    Keep the module docstring and all imports. Add MODELS_OPEN to config imports if not present
    (checkpointing uses LLM_MODELS, not training models — check existing imports).

    REPLACE add_checkpointing_arguments(parser, is_closed) with add_checkpointing_arguments(parser, mode):
    1. Create checkpointing_subparsers = parser.add_subparsers(dest="command", required=True)
    2. Create three subcommand parsers: datasize, run_benchmark, and configview (NEW):
         datasize     = checkpointing_subparsers.add_parser("datasize", ...)
         run_benchmark = checkpointing_subparsers.add_parser("run", ...)
         configview   = checkpointing_subparsers.add_parser("configview",
                          help=HELP_MESSAGES.get('configview', 'View final benchmark configuration'))
    3. For cmd_name, cmd_parser in [("datasize", datasize), ("run", run_benchmark),
                                     ("configview", configview)]:
         _add_checkpointing_core_args(cmd_parser, cmd_name)
         if mode in ("open", "whatif"):
             _add_checkpointing_open_args(cmd_parser, cmd_name)

    ADD _add_checkpointing_core_args(parser, command):
    - parser.set_defaults(loops=1, params='', allow_invalid_params=False,
                          num_checkpoints_read=10, num_checkpoints_write=10)
    - add_host_arguments(parser)
    - parser.add_argument('--client-host-memory-in-gb', '-cm', type=float, required=True,
        help=HELP_MESSAGES['client_host_mem_GB'])
    - parser.add_argument('--model', '-m', choices=LLM_MODELS, required=True,
        help="LLM model to benchmark")
      (Note: checkpointing keeps --model as a flag, not a positional)
    - parser.add_argument('--num-processes', '-np', type=int, required=True,
        help="Number of parallel processes")
    - parser.add_argument('--exec-type', '-et', ...)
    - add_mpi_arguments(parser)
    - add_universal_arguments(parser, req_results=(command in ("run", "configview")))
    - If command == "run":
        add_storage_type_arguments(parser)
        parser.add_argument('--checkpoint-folder', '-cf', required=True,
          help="Path to checkpoint storage folder")
    Examine the existing checkpointing_args.py carefully to carry forward all existing
    argument definitions (names, short flags, types, choices, defaults, help strings).

    ADD _add_checkpointing_open_args(parser, command):
    - parser.add_argument('--num-checkpoints-read', '-ncr', type=int, default=10, ...)
    - parser.add_argument('--num-checkpoints-write', '-ncw', type=int, default=10, ...)
    - parser.add_argument('--loops', type=int, default=1, ...)
    - parser.add_argument('--allow-invalid-params', '-aip', action='store_true', ...)
    - parser.add_argument('--params', '-p', nargs="+", action="append",
        metavar="KEY=VALUE", help=HELP_MESSAGES['params'])
    - add_dlio_arguments(parser)
    - If command == "run": add_timeseries_arguments(parser)

    RESOLVE conflict block (lines ~108-121): DELETE both sides. The three-tier calls above
    replace the old add_universal_arguments loop. No conflict markers in final file.

    Keep validate_checkpointing_arguments(args) unchanged.

    PART B — Update mlpstorage_py/benchmarks/dlio.py:

    Rename args.what_if to args.dry_run:
    - Find all occurrences of "what_if" in dlio.py (there is at least one at ~line 38:
      `if not getattr(args, 'what_if', False):`)
    - Replace every `what_if` with `dry_run` in dlio.py. Use getattr pattern with False default:
        getattr(args, 'what_if', False) → getattr(args, 'dry_run', False)

    Add configview dispatch to CheckpointingBenchmark._run():
    - Locate the command dispatch in CheckpointingBenchmark._run() — it is an if/elif chain
      or a dict mapping args.command to methods.
    - Add handling for args.command == "configview". If a _run_configview() method exists on
      the class (or parent), call it. If not, add a minimal implementation:
        elif args.command == "configview":
            cmd = self.generate_command() if hasattr(self, 'generate_command') else str(self.args)
            self.logger.status(f"Configuration view:\n{cmd}")
            return 0
      If TrainingBenchmark has a _run_configview() or _configview() method, look at its
      implementation and apply the same pattern to CheckpointingBenchmark.
  </action>

  <verify>
    <automated>python3 -c "
from mlpstorage_py.cli.checkpointing_args import add_checkpointing_arguments
import argparse, inspect
sig = inspect.signature(add_checkpointing_arguments)
params = list(sig.parameters)
assert params == ['parser', 'mode'], f'Wrong sig: {params}'
p = argparse.ArgumentParser()
add_checkpointing_arguments(p, 'closed')
print('checkpointing_args OK — closed branch built')
" && grep -c "is_closed" mlpstorage_py/cli/checkpointing_args.py | grep -q "^0$" && echo "No is_closed in checkpointing_args" && grep -c "what_if" mlpstorage_py/benchmarks/dlio.py | grep -q "^0$" && echo "No what_if in dlio.py"</automated>
  </verify>

  <acceptance_criteria>
    - `from mlpstorage_py.cli.checkpointing_args import add_checkpointing_arguments` succeeds
    - `inspect.signature(add_checkpointing_arguments)` parameters are ['parser', 'mode']
    - `grep "is_closed" mlpstorage_py/cli/checkpointing_args.py` returns 0 lines
    - `grep "<<<<<<\|>>>>>>" mlpstorage_py/cli/checkpointing_args.py` returns 0 lines
    - `grep "def _add_checkpointing_core_args" mlpstorage_py/cli/checkpointing_args.py` returns 1 line
    - `grep "def _add_checkpointing_open_args" mlpstorage_py/cli/checkpointing_args.py` returns 1 line
    - `grep '"configview"' mlpstorage_py/cli/checkpointing_args.py` returns at least 1 line
    - `grep "what_if" mlpstorage_py/benchmarks/dlio.py` returns 0 lines
    - `grep "dry_run" mlpstorage_py/benchmarks/dlio.py` returns at least 1 line
    - `grep "configview" mlpstorage_py/benchmarks/dlio.py` returns at least 1 line (in _run dispatch)
    - All three mode strings build without error: add_checkpointing_arguments(parser, 'closed'),
      add_checkpointing_arguments(parser, 'open'), add_checkpointing_arguments(parser, 'whatif')
  </acceptance_criteria>

  <done>
    checkpointing_args.py has mode-based API with configview subcommand; dlio.py has dry_run
    and configview dispatch; no is_closed anywhere in either file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closed branch → open args | Closed parsers must not register open-gated args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | _add_training_open_args called in closed branch | mitigate | Three-tier pattern: open/whatif builder functions are only called when mode in ("open","whatif"); argparse rejects unknown args |
| T-01-05 | Tampering | --checkpoint-folder required in run | accept | Required=True enforced by argparse; no security boundary — path validation is filesystem |
| T-01-06 | Information Disclosure | configview exposes generated DLIO command | accept | configview is an intentional read-only debug/inspection mode |
</threat_model>

<verification>
After both tasks complete:

1. `python3 -c "from mlpstorage_py.cli.training_args import add_training_arguments; from mlpstorage_py.cli.checkpointing_args import add_checkpointing_arguments"` exits 0
2. `grep -r "is_closed" mlpstorage_py/cli/training_args.py mlpstorage_py/cli/checkpointing_args.py` returns nothing
3. `grep -r "what_if" mlpstorage_py/benchmarks/dlio.py` returns nothing
4. Building each file's top-level function with mode='closed', 'open', 'whatif' raises no exceptions
</verification>

<success_criteria>
- add_training_arguments(parser, mode) — mode replaces is_closed throughout
- add_checkpointing_arguments(parser, mode) — same; configview subcommand added
- CheckpointingBenchmark._run() handles args.command == 'configview'
- dlio.py uses args.dry_run everywhere args.what_if was used
- No conflict markers remain in either arg builder file
</success_criteria>

<output>
Create `.planning/phases/01-core-cli-refactor/01-02-SUMMARY.md` when done
</output>
