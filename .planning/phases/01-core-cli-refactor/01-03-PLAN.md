---
phase: 01-core-cli-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - mlpstorage_py/cli/vectordb_args.py
  - mlpstorage_py/cli/kvcache_args.py
  - mlpstorage_py/benchmarks/kvcache.py
  - mlpstorage_py/benchmarks/vectordbbench.py
autonomous: true
requirements:
  - CLI-04
  - CLI-05
  - CLI-06
  - CLI-09

must_haves:
  truths:
    - "add_vectordb_arguments(parser, mode) accepts mode string, not is_closed boolean"
    - "vectordb --mode flag renamed to --benchmark-mode to avoid args.mode collision"
    - "add_kvcache_arguments(parser, mode) accepts mode string"
    - "kvcache closed branch has no model positional; open/whatif branch has model positional"
    - "add_storage_type_arguments is NOT called anywhere in kvcache_args.py"
    - "_add_kvcache_mlperf_arguments function exists in kvcache_args.py"
    - "No is_closed parameter in any vectordb or kvcache function signature"
    - "No conflict markers in vectordb_args.py or kvcache_args.py"
    - "args.what_if renamed to args.dry_run in kvcache.py and vectordbbench.py"
    - "kvcache.py args.closed replaced with args.mode == 'closed'"
  artifacts:
    - path: "mlpstorage_py/cli/vectordb_args.py"
      provides: "Three-tier vectordb arg builder"
      contains: "def add_vectordb_arguments(parser, mode)"
    - path: "mlpstorage_py/cli/kvcache_args.py"
      provides: "Three-tier kvcache arg builder with mlperf args and no storage type"
      contains: "def add_kvcache_arguments(parser, mode)"
    - path: "mlpstorage_py/benchmarks/kvcache.py"
      provides: "mode-based closed detection and dry_run rename"
      contains: "args.mode"
    - path: "mlpstorage_py/benchmarks/vectordbbench.py"
      provides: "dry_run rename"
      contains: "dry_run"
  key_links:
    - from: "mlpstorage_py/cli/vectordb_args.py"
      to: "mlpstorage_py/config.py"
      via: "import VDB_INDEX_TYPES_CLOSED, VDB_INDEX_TYPES"
      pattern: "from mlpstorage_py.config import"
    - from: "mlpstorage_py/benchmarks/kvcache.py"
      to: "mlpstorage_py/cli/kvcache_args.py"
      via: "is_closed derived from args.mode at runtime"
      pattern: "getattr.*mode"
---

<objective>
Rewrite vectordb_args.py and kvcache_args.py with the three-tier builder pattern, eliminating
is_closed from all function signatures. Update benchmarks/kvcache.py and
benchmarks/vectordbbench.py to read args.mode and args.dry_run.

Purpose: Completes the arg-builder layer. Runs in parallel with Plan 01-02 (different files).
kvcache has the most complex conflict resolution (3 blocks) and an architectural constraint
(no file/object positional).

Output: All four vectordb/kvcache source files updated; no is_closed anywhere; no conflict
markers; benchmark files use args.mode and args.dry_run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-cli-refactor/01-RESEARCH.md
@.planning/phases/01-core-cli-refactor/01-PATTERNS.md
@.planning/phases/01-core-cli-refactor/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From mlpstorage_py/cli/common_args.py after Plan 01-01 -->

add_universal_arguments(parser, req_results)
add_host_arguments(parser, required=False)
add_mpi_arguments(parser)
add_dlio_arguments(parser)
add_timeseries_arguments(parser)
add_storage_type_arguments(parser)

<!-- From mlpstorage_py/config.py -->
VDB_INDEX_TYPES        = ["DISKANN", "HNSW", "AISAQ", "IVF_FLAT", "IVF_SQ8", "FLAT"]
VDB_INDEX_TYPES_CLOSED = ["DISKANN", "HNSW", "AISAQ"]
KVCACHE_MODELS         = [...]   <!-- read from config.py -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite vectordb_args.py + update vectordbbench.py dry_run</name>
  <files>mlpstorage_py/cli/vectordb_args.py, mlpstorage_py/benchmarks/vectordbbench.py</files>

  <read_first>
    mlpstorage_py/cli/vectordb_args.py — read the full file:
      - Current add_vectordb_arguments(parser, is_closed) body
      - The conflict block (lines ~203-213)
      - The --mode flag on run_benchmark (line ~179: run_benchmark.add_argument('--mode', ...))
        — this must be renamed to --benchmark-mode (dest='benchmark_mode')
      - The index-type choices gating (if is_closed: → VDB_INDEX_TYPES_CLOSED; else → VDB_INDEX_TYPES)
      - All existing argument definitions (names, flags, choices, defaults)
      - validate_vectordb_arguments function
    mlpstorage_py/benchmarks/vectordbbench.py — read the full file:
      - The what_if reference at ~line 43: getattr(args, 'what_if', False)
    .planning/phases/01-core-cli-refactor/01-PATTERNS.md section "vectordb_args.py"
  </read_first>

  <action>
    PART A — Rewrite mlpstorage_py/cli/vectordb_args.py:

    Keep the module docstring and all imports.

    REPLACE add_vectordb_arguments(parser, is_closed) with add_vectordb_arguments(parser, mode):
    1. Determine index_choices: VDB_INDEX_TYPES_CLOSED if mode == "closed" else VDB_INDEX_TYPES
    2. Create vectordb_subparsers = parser.add_subparsers(dest="command", required=True)
    3. Create subcommand parsers: datasize, datagen, run_benchmark
       (examine the current file for the exact parser names and help strings used)
    4. For each cmd_name, cmd_parser in the appropriate list:
         _add_vectordb_core_args(cmd_parser, cmd_name, index_choices)
         if mode in ("open", "whatif"):
             _add_vectordb_open_args(cmd_parser, cmd_name)

    ADD _add_vectordb_core_args(parser, command, index_choices):
    - parser.set_defaults(loops=1, params='', allow_invalid_params=False)
    - Add all the core vectordb arguments — examine the current file carefully for:
        --num-clients, --num-queries (or equivalent), --algorithm or --index-type, etc.
    - IMPORTANT: On the run_benchmark parser, rename --mode to --benchmark-mode:
        parser.add_argument('--benchmark-mode', dest='benchmark_mode',
            choices=VDB_BENCHMARK_MODES, ...)
      (not --mode, because args.mode is now the top-level positional dest)
    - add_universal_arguments(parser, req_results=(command in ("datagen", "run")))
    - If command in ("datagen", "run"):
        add_storage_type_arguments(parser)
    Carry forward all existing argument definitions from the current file.

    ADD _add_vectordb_open_args(parser, command):
    - parser.add_argument('--loops', type=int, default=1, ...)
    - parser.add_argument('--allow-invalid-params', '-aip', action='store_true', ...)
    - parser.add_argument('--params', '-p', nargs="+", action="append",
        metavar="KEY=VALUE", help=HELP_MESSAGES['params'])
    - If command == "run": add_timeseries_arguments(parser)

    RESOLVE conflict block (lines ~203-213): DELETE both sides. The three-tier calls above
    replace the add_universal_arguments loop. No conflict markers in final file.

    Keep validate_vectordb_arguments(args) unchanged.

    PART B — Update mlpstorage_py/benchmarks/vectordbbench.py:
    - Locate the what_if reference at ~line 43: `if not getattr(args, 'what_if', False) and ...`
    - Replace `what_if` with `dry_run`: `if not getattr(args, 'dry_run', False) and ...`
    - Grep the file for any other `what_if` occurrences and replace them all.
  </action>

  <verify>
    <automated>python3 -c "
from mlpstorage_py.cli.vectordb_args import add_vectordb_arguments
import argparse, inspect
sig = inspect.signature(add_vectordb_arguments)
params = list(sig.parameters)
assert params == ['parser', 'mode'], f'Wrong sig: {params}'
p = argparse.ArgumentParser()
add_vectordb_arguments(p, 'closed')
print('vectordb_args OK')
" && grep -c "is_closed" mlpstorage_py/cli/vectordb_args.py | grep -q "^0$" && echo "No is_closed" && grep -c "what_if" mlpstorage_py/benchmarks/vectordbbench.py | grep -q "^0$" && echo "No what_if in vectordbbench"</automated>
  </verify>

  <acceptance_criteria>
    - `from mlpstorage_py.cli.vectordb_args import add_vectordb_arguments` succeeds
    - `inspect.signature(add_vectordb_arguments)` parameters are ['parser', 'mode']
    - `grep "is_closed" mlpstorage_py/cli/vectordb_args.py` returns 0 lines
    - `grep "<<<<<<\|>>>>>>" mlpstorage_py/cli/vectordb_args.py` returns 0 lines
    - `grep "\-\-benchmark-mode" mlpstorage_py/cli/vectordb_args.py` returns at least 1 line
    - `grep "'\-\-mode'" mlpstorage_py/cli/vectordb_args.py` returns 0 lines (renamed to --benchmark-mode)
    - `grep "what_if" mlpstorage_py/benchmarks/vectordbbench.py` returns 0 lines
    - `grep "dry_run" mlpstorage_py/benchmarks/vectordbbench.py` returns at least 1 line
    - Building add_vectordb_arguments(parser, 'closed'), (parser, 'open'), (parser, 'whatif') all succeed
  </acceptance_criteria>

  <done>
    vectordb_args.py has mode-based API; --mode renamed to --benchmark-mode; three-tier pattern applied;
    vectordbbench.py uses dry_run
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite kvcache_args.py (3 conflict blocks, mode param) + update kvcache.py</name>
  <files>mlpstorage_py/cli/kvcache_args.py, mlpstorage_py/benchmarks/kvcache.py</files>

  <read_first>
    mlpstorage_py/cli/kvcache_args.py — read the full file very carefully:
      - The three conflict blocks: lines ~109-129, ~202-211, ~239-288
      - Every function signature (6 functions with is_closed)
      - The existing _add_kvcache_mlperf_arguments content from origin/main side of block 3
        (lines ~243-284 of current file) — this becomes the body of the new standalone function
      - What arguments _add_kvcache_cache_arguments, _add_kvcache_run_arguments,
        _add_kvcache_optional_features currently contain
      - The KVCACHE_MODELS, KVCACHE_DEFAULT_DURATION, KVCACHE_GENERATION_MODES,
        KVCACHE_PERFORMANCE_PROFILES constants used
    mlpstorage_py/benchmarks/kvcache.py — read the full file:
      - Line ~183: is_closed = getattr(self.args, 'closed', False)
      - Lines ~269, 274, 279, 331-332: what_if references
    .planning/phases/01-core-cli-refactor/01-PATTERNS.md section "kvcache_args.py"
    .planning/phases/01-core-cli-refactor/01-RESEARCH.md section "Exact Conflict Blocks"
  </read_first>

  <action>
    PART A — Rewrite mlpstorage_py/cli/kvcache_args.py:

    Keep the module docstring and all imports.

    REPLACE all six function signatures that contain is_closed:

    add_kvcache_arguments(parser, is_closed) → add_kvcache_arguments(parser, mode)
    _add_kvcache_model_arguments(parser, is_closed) → _add_kvcache_model_arguments(parser)
    _add_kvcache_cache_arguments(parser, is_closed) → _add_kvcache_cache_arguments(parser, mode)
    _add_kvcache_run_arguments(parser, is_closed) → _add_kvcache_run_arguments(parser)
    _add_kvcache_optional_features(parser, is_closed) → _add_kvcache_optional_features(parser, mode)
    _add_kvcache_distributed_arguments(parser, is_closed) → _add_kvcache_distributed_arguments(parser)

    NEW FUNCTION — add _add_kvcache_open_args(parser): collects open/whatif-only kvcache args.
    Extract from the existing body of _add_kvcache_cache_arguments and
    _add_kvcache_run_arguments the arguments that are currently if not is_closed: gated.
    These move into _add_kvcache_open_args:
    - --duration, -d (type=int, default=KVCACHE_DEFAULT_DURATION)
    - --generation-mode (choices=KVCACHE_GENERATION_MODES)
    - --performance-profile (choices=KVCACHE_PERFORMANCE_PROFILES)
    - --loops, type=int, default=1
    - --allow-invalid-params, -aip, action='store_true'

    NEW FUNCTION — add _add_kvcache_mlperf_arguments(parser): add this as a standalone
    function. Extract its body from the origin/main side of conflict block 3 (lines ~243-284
    of current file). This is only called for open/whatif branches. It contains arguments
    like --seed, --trials, --inter-option-delay.

    CONFLICT BLOCK 1 (lines ~109-129): DELETE both sides; replace with:
      for _parser in [run_benchmark, datasize]:
          _add_kvcache_cache_arguments(_parser, mode)
          add_universal_arguments(_parser, req_results=(_parser is run_benchmark))
          # NO add_storage_type_arguments — kvcache has no file/object positional
      _add_kvcache_run_arguments(run_benchmark)
      if mode in ("open", "whatif"):
          _add_kvcache_model_arguments(run_benchmark)
          _add_kvcache_open_args(run_benchmark)
          _add_kvcache_mlperf_arguments(run_benchmark)
      _add_kvcache_optional_features(run_benchmark, mode)
      _add_kvcache_distributed_arguments(run_benchmark)

    CONFLICT BLOCK 2 (lines ~202-211): DELETE both sides.
    The --seed argument moves to _add_kvcache_mlperf_arguments (open/whatif only).
    The duplicate --kvcache-bin-path from origin/main is a bug — do not include it.

    CONFLICT BLOCK 3 (lines ~239-288):
    Extract _add_kvcache_mlperf_arguments body from the origin/main side verbatim.
    For _add_kvcache_optional_features: replace is_closed parameter with mode parameter.
    Replace all `is_closed` references inside the body with `mode == "closed"`.

    For closed mode defaults — in add_kvcache_arguments or _add_kvcache_cache_arguments,
    when mode == "closed", call parser.set_defaults with the hardcoded closed values for
    all open-gated attributes (loops=1, duration=KVCACHE_DEFAULT_DURATION,
    generation_mode='realistic', performance_profile='latency', gpu_mem_gb=16.0,
    cpu_mem_gb=32.0, disable_multi_turn=False, disable_prefix_caching=False,
    enable_rag=True, rag_num_docs=10, enable_autoscaling=True, autoscaler_mode='qos',
    seed=42, trials=3, inter_option_delay=20, allow_invalid_params=False, params='').
    In open/whatif mode, these attrs are registered as actual arguments.

    ARCHITECTURAL CONSTRAINT: add_storage_type_arguments must NOT be called anywhere in
    kvcache_args.py. kvcache has no file/object storage type positional by design.
    Verify by grep after editing.

    PART B — Update mlpstorage_py/benchmarks/kvcache.py:

    Replace args.closed with args.mode check:
    - Line ~183: `is_closed = getattr(self.args, 'closed', False)`
      → `is_closed = (getattr(self.args, 'mode', None) == 'closed')`

    Replace what_if with dry_run:
    - Lines ~269, 274, 279, 331-332: replace every `what_if` with `dry_run`.
      `getattr(self.args, 'what_if', False)` → `getattr(self.args, 'dry_run', False)`
      `not getattr(self.args, 'what_if', False)` → `not getattr(self.args, 'dry_run', False)`

    Grep the full file for any remaining `what_if` or `closed` attribute access patterns
    and update them all.
  </action>

  <verify>
    <automated>python3 -c "
from mlpstorage_py.cli.kvcache_args import add_kvcache_arguments, _add_kvcache_mlperf_arguments
import argparse, inspect
sig = inspect.signature(add_kvcache_arguments)
params = list(sig.parameters)
assert params == ['parser', 'mode'], f'Wrong sig: {params}'
p = argparse.ArgumentParser()
add_kvcache_arguments(p, 'closed')
print('kvcache_args closed branch OK')
p2 = argparse.ArgumentParser()
add_kvcache_arguments(p2, 'open')
print('kvcache_args open branch OK')
" && grep -c "is_closed" mlpstorage_py/cli/kvcache_args.py | grep -q "^0$" && echo "No is_closed" && grep "add_storage_type_arguments" mlpstorage_py/cli/kvcache_args.py | grep -v "^#" | wc -l | grep -q "^0$" && echo "No add_storage_type_arguments in kvcache" && grep -c "what_if\|args\.closed" mlpstorage_py/benchmarks/kvcache.py | grep -q "^0$" && echo "kvcache.py updated"</automated>
  </verify>

  <acceptance_criteria>
    - `from mlpstorage_py.cli.kvcache_args import add_kvcache_arguments` succeeds
    - `inspect.signature(add_kvcache_arguments)` parameters are ['parser', 'mode']
    - `grep "is_closed" mlpstorage_py/cli/kvcache_args.py` returns 0 lines
    - `grep "<<<<<<\|>>>>>>" mlpstorage_py/cli/kvcache_args.py` returns 0 lines
    - `grep "def _add_kvcache_mlperf_arguments" mlpstorage_py/cli/kvcache_args.py` returns 1 line
    - `grep "def _add_kvcache_open_args" mlpstorage_py/cli/kvcache_args.py` returns 1 line
    - `grep -v "^#" mlpstorage_py/cli/kvcache_args.py | grep "add_storage_type_arguments"` returns 0 lines
    - Building add_kvcache_arguments(parser, 'closed') and add_kvcache_arguments(parser, 'open') both succeed
    - `grep "args\.closed" mlpstorage_py/benchmarks/kvcache.py` returns 0 lines
    - `grep "what_if" mlpstorage_py/benchmarks/kvcache.py` returns 0 lines
    - `grep "dry_run" mlpstorage_py/benchmarks/kvcache.py` returns at least 1 line
    - `grep "args\.mode" mlpstorage_py/benchmarks/kvcache.py` returns at least 1 line
  </acceptance_criteria>

  <done>
    kvcache_args.py: three-tier pattern with _add_kvcache_open_args and _add_kvcache_mlperf_arguments;
    no is_closed; no storage type; no conflict markers. kvcache.py: args.mode + args.dry_run throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kvcache closed → open args | kvcache closed must not expose open-gated run configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Elevation of Privilege | kvcache --allow-invalid-params in closed | mitigate | _add_kvcache_open_args not called in closed branch; argparse rejects unknown arg |
| T-01-08 | Tampering | kvcache no file/object positional constraint | accept | Architectural constraint enforced by not calling add_storage_type_arguments — no user bypass possible |
| T-01-09 | Spoofing | vectordb --mode renamed to --benchmark-mode | accept | Rename prevents args.mode collision; no security consequence — just correctness |
</threat_model>

<verification>
After both tasks complete:

1. `python3 -c "from mlpstorage_py.cli.vectordb_args import add_vectordb_arguments; from mlpstorage_py.cli.kvcache_args import add_kvcache_arguments"` exits 0
2. `grep -r "is_closed" mlpstorage_py/cli/vectordb_args.py mlpstorage_py/cli/kvcache_args.py` returns nothing
3. `grep -r "what_if\|args\.closed" mlpstorage_py/benchmarks/kvcache.py mlpstorage_py/benchmarks/vectordbbench.py` returns nothing
4. `grep "add_storage_type_arguments" mlpstorage_py/cli/kvcache_args.py` returns nothing
</verification>

<success_criteria>
- add_vectordb_arguments(parser, mode) — is_closed removed; --mode renamed to --benchmark-mode
- add_kvcache_arguments(parser, mode) — is_closed removed; 3 conflict blocks resolved; no file/object
- _add_kvcache_mlperf_arguments and _add_kvcache_open_args functions exist
- kvcache.py and vectordbbench.py use args.mode and args.dry_run
- No conflict markers in any of the four files
</success_criteria>

<output>
Create `.planning/phases/01-core-cli-refactor/01-03-SUMMARY.md` when done
</output>
