---
phase: 01-core-cli-refactor
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - mlpstorage_py/cli_parser.py
  - mlpstorage_py/main.py
  - mlpstorage_py/benchmarks/base.py
  - tests/unit/test_cli_parser.py
  - tests/unit/test_cli.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-06
  - CLI-08

must_haves:
  truths:
    - "parse_arguments() builds three top-level mode subparsers (closed, open, whatif)"
    - "Utility subparsers (reports, history, lockfile) are top-level siblings of mode subparsers"
    - "args.mode holds the top-level positional: closed/open/whatif/reports/history/lockfile"
    - "args.benchmark holds the benchmark name for benchmark modes"
    - "args.model holds the training model positional"
    - "No sys.argv.pop() or manual mode detection remains in cli_parser.py"
    - "mlpstorage closed training unet3d run --file parses without error (args.mode=='closed', args.benchmark=='training', args.model=='unet3d', args.command=='run')"
    - "mlpstorage closed training unet3d run --loops 2 raises SystemExit (unrecognized arg)"
    - "main.py dispatch uses args.mode for utilities and args.benchmark for benchmark classes"
    - "base.py reads args.mode to derive closed_mode and open_mode booleans"
    - "test_cli_parser.py and test_cli.py pass after being updated to the new argument structure"
  artifacts:
    - path: "mlpstorage_py/cli_parser.py"
      provides: "Three-branch positional argparse tree"
      contains: "def _build_mode_branch"
    - path: "mlpstorage_py/main.py"
      provides: "Updated dispatch using args.mode and args.benchmark"
      contains: "args.benchmark"
    - path: "mlpstorage_py/benchmarks/base.py"
      provides: "Mode detection using args.mode"
      contains: "mode == 'closed'"
    - path: "tests/unit/test_cli_parser.py"
      provides: "Updated tests for positional argument structure"
      contains: "args.mode"
  key_links:
    - from: "mlpstorage_py/cli_parser.py"
      to: "mlpstorage_py/cli/__init__.py"
      via: "imports all arg builders by name"
      pattern: "from mlpstorage_py.cli import"
    - from: "mlpstorage_py/main.py"
      to: "mlpstorage_py/cli_parser.py"
      via: "parse_arguments() returns args with args.mode and args.benchmark"
      pattern: "args\\.benchmark"
    - from: "mlpstorage_py/benchmarks/base.py"
      to: "args namespace"
      via: "getattr(self.args, 'mode', None) comparison"
      pattern: "args.*mode.*closed"
---

<objective>
Rewrite cli_parser.py as the new three-branch positional parser. Update main.py dispatch
and base.py mode detection to use args.mode/args.benchmark. Update existing unit tests to
match the new argument structure.

Purpose: This is the capstone plan — wires together all the arg builders into a working
CLI. Depends on all wave-2 plans completing first.

Output: A working mlpstorage CLI with three-branch positional structure; updated unit tests
that pass against the new structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-cli-refactor/01-RESEARCH.md
@.planning/phases/01-core-cli-refactor/01-PATTERNS.md
@.planning/phases/01-core-cli-refactor/01-01-SUMMARY.md
@.planning/phases/01-core-cli-refactor/01-02-SUMMARY.md
@.planning/phases/01-core-cli-refactor/01-03-SUMMARY.md
@.planning/phases/01-core-cli-refactor/01-04-SUMMARY.md
</context>

<interfaces>
<!-- All arg builders after wave-2 plans complete -->

add_training_arguments(parser, mode)         <!-- mode: 'closed'|'open'|'whatif' -->
add_checkpointing_arguments(parser, mode)
add_vectordb_arguments(parser, mode)
add_kvcache_arguments(parser, mode)
add_reports_arguments(parser)
add_history_arguments(parser)
add_lockfile_arguments(parser)

<!-- validate_* functions -->
validate_training_arguments(args)
validate_checkpointing_arguments(args)
validate_vectordb_arguments(args)
validate_kvcache_arguments(args)

<!-- args namespace after parse_arguments() -->
args.mode      — 'closed'|'open'|'whatif'|'reports'|'history'|'lockfile'
args.benchmark — 'training'|'checkpointing'|'vectordb'|'kvcache'  (absent for utilities)
args.command   — 'run'|'datagen'|'datasize'|'configview'
args.model     — model string (training positional)
args.dry_run   — bool (replaces args.what_if)

<!-- main.py dispatch dict -->
program_switch_dict = dict(training=TrainingBenchmark, checkpointing=CheckpointingBenchmark,
                           vectordb=VectorDBBenchmark, kvcache=KVCacheBenchmark)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite cli_parser.py with three-branch positional structure</name>
  <files>mlpstorage_py/cli_parser.py</files>

  <read_first>
    mlpstorage_py/cli_parser.py — read the full file:
      - Lines 1-50: module imports, VERSION usage, current parse_arguments() start
      - Lines 34-171: the current parse_arguments() with sys.argv.pop() hack (to be replaced)
      - Lines 174-235: apply_yaml_config_overrides (keep UNCHANGED)
      - Lines 241-255: validate_args (update args.program → args.benchmark dispatch)
      - Lines 258-313: update_args (keep UNCHANGED — uses hasattr guards)
    .planning/phases/01-core-cli-refactor/01-PATTERNS.md section "cli_parser.py"
    .planning/phases/01-core-cli-refactor/01-RESEARCH.md sections "Architecture Patterns" and "Dispatch Impact"
  </read_first>

  <action>
    Rewrite parse_arguments() and add the _build_mode_branch() helper.

    KEEP UNCHANGED (do not modify these parts of cli_parser.py):
    - Module docstring, all imports
    - apply_yaml_config_overrides() function (lines ~174-235)
    - update_args() function (lines ~258-313)
    - The VERSION constant usage

    REPLACE parse_arguments() (lines ~34-171) with this structure:

    def parse_arguments():
        parser = argparse.ArgumentParser(
            prog="mlpstorage",
            description="Script to launch the MLPerf Storage benchmark"
        )
        parser.add_argument("--version", action="version", version=f"%(prog)s {VERSION}")

        top = parser.add_subparsers(dest="mode", required=True)

        # Three benchmark mode branches
        for mode_name in ("closed", "open", "whatif"):
            mode_parser = top.add_parser(
                mode_name,
                description=f"Run benchmarks in {mode_name} configuration",
                help=f"{mode_name.capitalize()} submission mode"
            )
            _build_mode_branch(mode_parser, mode_name)

        # Utility siblings — top-level, not nested under modes
        reports_parser  = top.add_parser("reports",  help="Generate a report from benchmark results")
        history_parser  = top.add_parser("history",  help="Display benchmark history")
        lockfile_parser = top.add_parser("lockfile", help="Generate and verify package lockfiles")
        add_reports_arguments(reports_parser)
        add_history_arguments(history_parser)
        add_lockfile_arguments(lockfile_parser)

        parsed_args = parser.parse_args()

        # Consolidate --file/--object flags into data_access_protocol
        # (mirrors the existing consolidation logic from the old parse_arguments)
        if getattr(parsed_args, 'file', False):
            parsed_args.data_access_protocol = 'file'
            del parsed_args.file
            if hasattr(parsed_args, 'object'):
                del parsed_args.object
        elif getattr(parsed_args, 'object', None) is not None:
            parsed_args.data_access_protocol = parsed_args.object
            del parsed_args.object
            if hasattr(parsed_args, 'file'):
                del parsed_args.file

        validate_args(parsed_args)
        return parsed_args


    ADD _build_mode_branch(mode_parser, mode):
        benchmarks = mode_parser.add_subparsers(dest="benchmark", required=True)

        training_parser      = benchmarks.add_parser("training",
            help="Training benchmark (unet3d, resnet50, etc.)")
        checkpointing_parser = benchmarks.add_parser("checkpointing",
            help="Checkpointing benchmark (llama3-8b, llama3-70b, etc.)")
        vectordb_parser      = benchmarks.add_parser("vectordb",
            help="Vector database benchmark (PREVIEW)")
        kvcache_parser       = benchmarks.add_parser("kvcache",
            help="KV-cache benchmark for LLM inference")

        add_training_arguments(training_parser, mode)
        add_checkpointing_arguments(checkpointing_parser, mode)
        add_vectordb_arguments(vectordb_parser, mode)
        add_kvcache_arguments(kvcache_parser, mode)

    UPDATE validate_args() — change args.program dispatch to args.benchmark:
    The function currently uses args.program to dispatch to validate_training_arguments etc.
    Change every `args.program` reference to use `getattr(args, 'benchmark', None)`.
    New form:
        def validate_args(args):
            benchmark = getattr(args, 'benchmark', None)
            if benchmark == 'training':
                validate_training_arguments(args)
            if benchmark == 'checkpointing':
                validate_checkpointing_arguments(args)
            if benchmark == 'vectordb':
                validate_vectordb_arguments(args)
            if benchmark == 'kvcache':
                validate_kvcache_arguments(args)
    Keep any other logic currently in validate_args (e.g., apply_yaml_config_overrides call
    if it is in validate_args — check the actual file).

    REMOVE the sys.argv.pop() block and is_open/is_closed boolean setup entirely.
    REMOVE the post-parse `parsed_args.open = is_open` and `parsed_args.closed = is_closed` lines.
    These are replaced by args.mode from the subparser dest.
  </action>

  <verify>
    <automated>python3 -c "
import sys
from unittest.mock import patch
from mlpstorage_py.cli_parser import parse_arguments

# Test closed training run
with patch('sys.argv', ['mlpstorage', 'closed', 'training', 'unet3d', 'run',
                         '--data-dir', '/tmp', '--results-dir', '/tmp',
                         '--num-accelerators', '1', '--accelerator-type', 'b200',
                         '--client-host-memory-in-gb', '64', '--file']):
    try:
        args = parse_arguments()
        assert args.mode == 'closed', f'mode={args.mode}'
        assert args.benchmark == 'training', f'benchmark={args.benchmark}'
        assert args.model == 'unet3d', f'model={args.model}'
        assert args.command == 'run', f'command={args.command}'
        print(f'Closed training run: OK (mode={args.mode}, benchmark={args.benchmark}, model={args.model})')
    except SystemExit as e:
        print(f'FAIL: SystemExit {e}')
        sys.exit(1)

# Test that open-gated arg in closed mode raises
import pytest
with patch('sys.argv', ['mlpstorage', 'closed', 'training', 'unet3d', 'run',
                         '--data-dir', '/tmp', '--results-dir', '/tmp',
                         '--num-accelerators', '1', '--accelerator-type', 'b200',
                         '--client-host-memory-in-gb', '64', '--file',
                         '--loops', '2']):
    try:
        args = parse_arguments()
        print('FAIL: should have raised SystemExit for --loops in closed mode')
        sys.exit(1)
    except SystemExit as e:
        if e.code != 0:
            print(f'Closed mode rejects --loops: OK (exit code {e.code})')
        else:
            print('FAIL: exit code 0 (should be non-zero)')
            sys.exit(1)
"</automated>
  </verify>

  <acceptance_criteria>
    - `from mlpstorage_py.cli_parser import parse_arguments` succeeds without ImportError
    - `grep "sys.argv.pop\|argv\[1\]\|is_open\|is_closed" mlpstorage_py/cli_parser.py` returns 0 lines
    - `grep "def _build_mode_branch" mlpstorage_py/cli_parser.py` returns 1 line
    - `grep "dest=\"mode\"" mlpstorage_py/cli_parser.py` returns at least 1 line
    - `grep "dest=\"benchmark\"" mlpstorage_py/cli_parser.py` returns at least 1 line
    - Parsing `mlpstorage closed training unet3d run --data-dir /tmp --results-dir /tmp --num-accelerators 1 --accelerator-type b200 --client-host-memory-in-gb 64 --file` produces args.mode=='closed', args.benchmark=='training', args.model=='unet3d'
    - Parsing `mlpstorage closed training unet3d run ... --loops 2` raises SystemExit with non-zero code
    - `grep "args.program" mlpstorage_py/cli_parser.py` returns 0 lines (all replaced with args.benchmark or args.mode)
  </acceptance_criteria>

  <done>
    cli_parser.py rewrites complete: three-branch positional tree; no sys.argv.pop; args.mode and
    args.benchmark set correctly; validate_args dispatches on args.benchmark; closed mode rejects open args
  </done>
</task>

<task type="auto">
  <name>Task 2: Update main.py + base.py dispatch + fix existing unit tests</name>
  <files>mlpstorage_py/main.py, mlpstorage_py/benchmarks/base.py, tests/unit/test_cli_parser.py, tests/unit/test_cli.py</files>

  <read_first>
    mlpstorage_py/main.py — read lines 195-315 (the _main_impl function):
      - Lines 205-215: program_switch_dict.get(args.program) — must become args.benchmark
      - Lines 276-309: all args.program comparisons — must become args.mode
      - Line 289: hasattr(new_args, 'program') — must become hasattr(new_args, 'mode')
    mlpstorage_py/benchmarks/base.py — read targeted sections:
      - Lines ~826-827: closed_mode = getattr(self.args, 'closed', False)
      - Line ~265: if self.args.what_if:
      - Line ~661: if hasattr(self.args, 'what_if') and self.args.what_if:
    tests/unit/test_cli_parser.py — read the full file to understand current test structure
    tests/unit/test_cli.py — read the full file to understand current test structure
    .planning/phases/01-core-cli-refactor/01-PATTERNS.md sections "main.py", "base.py", "Tests to Update"
  </read_first>

  <action>
    PART A — Update mlpstorage_py/main.py:

    Replace all args.program attribute reads with the appropriate new attribute:
    - In run_benchmark() function (line ~205):
        `program_switch_dict.get(args.program)` → `program_switch_dict.get(args.benchmark)`
    - Error message ConfigurationError (lines ~208-215):
        `f"Unsupported benchmark type: {args.program}"` → `f"Unsupported benchmark type: {args.benchmark}"`
        `parameter="program"` → `parameter="benchmark"`
        `actual=args.program` → `actual=args.benchmark`

    - In _main_impl() function:
        `if args.program != "history":` → `if args.mode != "history":`
        `if args.program == 'history':` → `if args.mode == 'history':`
        `elif isinstance(new_args, object) and hasattr(new_args, 'program'):` → `hasattr(new_args, 'mode')`
        `if args.program == "lockfile":` → `if args.mode == "lockfile":`
        `if args.program == "reports":` → `if args.mode == "reports":`

    Grep for any remaining `args.program` in main.py and update every occurrence.

    PART B — Update mlpstorage_py/benchmarks/base.py:

    Replace closed/open mode detection (lines ~826-827):
        OLD: closed_mode = getattr(self.args, 'closed', False)
             open_mode = getattr(self.args, 'open', False)
        NEW: mode = getattr(self.args, 'mode', None)
             closed_mode = (mode == 'closed')
             open_mode   = (mode == 'open')

    Replace dry_run / what_if (lines ~265 and ~661):
        OLD: if self.args.what_if:
        NEW: if getattr(self.args, 'dry_run', False):

        OLD: if hasattr(self.args, 'what_if') and self.args.what_if:
        NEW: if getattr(self.args, 'dry_run', False):

    Grep base.py for all remaining `what_if`, `args.closed`, `args.open` occurrences
    and update them. The file has at least 2 what_if references and at least 2 closed/open references.

    PART C — Update tests/unit/test_cli_parser.py:

    The test file currently fails at collection because common_args.py had a SyntaxError.
    After Plan 01-01 that SyntaxError is resolved, but the tests themselves reference the OLD
    argument structure. Update them to match the new structure:

    - `args.closed` reads → `args.mode == "closed"`
    - `args.open` reads → `args.mode == "open"`
    - `args.program` reads → `args.benchmark` (for benchmark commands) or `args.mode` (for utilities)
    - `['--model', 'unet3d']` in sys.argv lists → `'unet3d'` as a positional (no --model flag for training)
    - `['--what-if']` → `['--dry-run']`
    - CLI invocations like `['mlpstorage', 'closed', 'training', 'run', ...]` must include
      the model positional: `['mlpstorage', 'closed', 'training', 'unet3d', 'run', ...]`
    - `['mlpstorage', 'closed', 'kvcache', 'run', ...]` stays as-is (kvcache closed has no model positional)
    - For checkpointing: `['mlpstorage', 'closed', 'checkpointing', 'run', '-m', 'llama3-8b', ...]`
      stays as-is (checkpointing keeps --model as a flag)

    Read the test file in full, then update each test method to use the new invocation syntax.
    Keep the test class structure and test method names unchanged — only update sys.argv
    contents and assertion attribute names.

    PART D — Update tests/unit/test_cli.py:

    Read the full file and identify:
    - Calls to add_universal_arguments with old 5-param signature → update to 2-param
    - Calls to add_training_arguments(parser) or add_training_arguments(parser, is_closed)
      → update to add_training_arguments(parser, 'closed') or appropriate mode
    - Tests for --closed and --open flags on universal args parser → remove (flags no longer exist)
    - Tests for --what-if flag → change to --dry-run

    Update each affected test to use the new API. Remove tests that test behaviors that
    no longer exist (e.g., --open/--closed as flags). Do NOT remove valid tests that cover
    behaviors still present after the refactor (--dry-run, req_results behavior, etc.).
  </action>

  <verify>
    <automated>pytest tests/unit/test_cli_parser.py tests/unit/test_cli.py -v -x 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep "args\.program" mlpstorage_py/main.py` returns 0 lines
    - `grep "args\.closed\|args\.open" mlpstorage_py/benchmarks/base.py` returns 0 lines
    - `grep "what_if" mlpstorage_py/benchmarks/base.py` returns 0 lines
    - `pytest tests/unit/test_cli_parser.py -v` passes (0 failures)
    - `pytest tests/unit/test_cli.py -v` passes (0 failures)
    - `pytest tests/unit/ -v` passes for all tests not previously failing due to pyarrow/psutil import issues
    - `mlpstorage closed training unet3d run --help` (via subprocess or patch) exits 0 and shows flags without --loops or --params
    - `mlpstorage open training unet3d run --help` exits 0 and shows --loops flag
  </acceptance_criteria>

  <done>
    main.py uses args.mode and args.benchmark throughout; base.py uses mode string comparison;
    both test files pass; no args.program, args.closed, args.open, or args.what_if remain in
    any non-test source file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI → argparse | Top-level mode subparser is the first trust boundary |
| argparse namespace → main.py dispatch | args.benchmark must be validated before lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | program_switch_dict lookup on args.benchmark | mitigate | Dict lookup returns None for unknown keys; ConfigurationError raised immediately |
| T-01-13 | Spoofing | args.mode vs args.program transition | mitigate | Grep gate: zero remaining args.program references in main.py before task done |
| T-01-14 | Denial of Service | Broken test suite blocks verification | mitigate | test_cli_parser.py and test_cli.py updated as part of this task; full suite runs in verify step |
</threat_model>

<verification>
After both tasks complete:

1. `python3 -c "from mlpstorage_py.cli_parser import parse_arguments; from mlpstorage_py.main import _main_impl"` exits 0
2. `grep -r "args\.program" mlpstorage_py/main.py mlpstorage_py/cli_parser.py` returns nothing
3. `grep "args\.closed\|args\.open\|what_if" mlpstorage_py/benchmarks/base.py` returns nothing
4. `pytest tests/unit/ -v` green for all non-infrastructure tests
5. Parsing `mlpstorage closed training unet3d run --data-dir /tmp --results-dir /tmp --num-accelerators 1 --accelerator-type b200 --client-host-memory-in-gb 64 --file` produces args.mode=='closed', args.benchmark=='training', args.model=='unet3d', args.command=='run'
</verification>

<success_criteria>
- mlpstorage CLI fully functional with three-branch positional structure
- No args.program, args.closed, args.open, or args.what_if in any source file
- test_cli_parser.py and test_cli.py pass
- Closed mode rejects open-gated args (--loops, --params, --allow-invalid-params)
- Utility commands (reports, history, lockfile) work as top-level siblings
</success_criteria>

<output>
Create `.planning/phases/01-core-cli-refactor/01-05-SUMMARY.md` when done
</output>
