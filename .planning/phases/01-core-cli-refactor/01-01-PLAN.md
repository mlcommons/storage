---
phase: 01-core-cli-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mlpstorage_py/config.py
  - mlpstorage_py/cli/common_args.py
autonomous: true
requirements:
  - CLI-04
  - CLI-07
  - CLI-10

must_haves:
  truths:
    - "common_args.py imports without SyntaxError"
    - "No is_closed parameter in any common_args.py function signature"
    - "No conflict markers (<<<<<<< / >>>>>>> / =======) in common_args.py"
    - "add_storage_type_arguments exists as a standalone function in common_args.py"
    - "MODELS_CLOSED equals [UNET, RETINANET] in config.py"
    - "MODELS_OPEN constant exists in config.py with value [UNET, RETINANET]"
  artifacts:
    - path: "mlpstorage_py/cli/common_args.py"
      provides: "Conflict-free common arg builders with simplified signatures"
      contains: "def add_universal_arguments(parser, req_results)"
    - path: "mlpstorage_py/config.py"
      provides: "Corrected model constants"
      contains: "MODELS_OPEN"
  key_links:
    - from: "mlpstorage_py/cli/training_args.py"
      to: "mlpstorage_py/cli/common_args.py"
      via: "import add_universal_arguments, add_storage_type_arguments"
      pattern: "from mlpstorage_py.cli.common_args import"
    - from: "mlpstorage_py/cli/training_args.py"
      to: "mlpstorage_py/config.py"
      via: "import MODELS_CLOSED, MODELS_OPEN"
      pattern: "from mlpstorage_py.config import"
---

<objective>
Resolve the SyntaxError blocker caused by raw merge conflict markers in common_args.py, and
correct the config.py model constants. These two changes unblock all subsequent wave-2 plans.

Purpose: Without this plan, every CLI import raises SyntaxError and no other plan can be
tested. config.py has no import dependencies on cli, so it can be fixed in the same wave.

Output: A parseable common_args.py with simplified function signatures; config.py with
correct MODELS_CLOSED and new MODELS_OPEN constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-cli-refactor/01-RESEARCH.md
@.planning/phases/01-core-cli-refactor/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix config.py — correct MODELS_CLOSED and add MODELS_OPEN</name>
  <files>mlpstorage_py/config.py</files>

  <read_first>
    mlpstorage_py/config.py — read the full file to see current constant definitions
    (UNET, RETINANET, COSMOFLOW, RESNET, DLRM, FLUX constants and MODELS, MODELS_CLOSED lines)
  </read_first>

  <action>
    In mlpstorage_py/config.py, locate the MODELS_CLOSED constant (currently line 55, value
    [DLRM, RETINANET, FLUX]).

    Make these two edits:
    1. Replace the MODELS_CLOSED line with:
         MODELS_CLOSED = [UNET, RETINANET]
    2. Insert a new line immediately after MODELS_CLOSED:
         MODELS_OPEN   = [UNET, RETINANET]

    The MODELS constant (the whatif list: [COSMOFLOW, RESNET, UNET, DLRM, RETINANET, FLUX])
    must remain unchanged on the line that follows MODELS_OPEN.

    Do NOT modify any other constants. Do NOT add or remove any imports.
    The ACCELERATORS_CLOSED constant ([B200, MI355]) is already correct — leave it alone.
  </action>

  <verify>
    <automated>python3 -c "from mlpstorage_py.config import MODELS_CLOSED, MODELS_OPEN, MODELS, UNET, RETINANET; assert set(MODELS_CLOSED) == {'unet3d', 'retinanet'}, f'MODELS_CLOSED wrong: {MODELS_CLOSED}'; assert set(MODELS_OPEN) == {'unet3d', 'retinanet'}, f'MODELS_OPEN wrong: {MODELS_OPEN}'; print('config.py constants OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `from mlpstorage_py.config import MODELS_CLOSED, MODELS_OPEN` succeeds without ImportError
    - `MODELS_CLOSED` equals `['unet3d', 'retinanet']` (UNET and RETINANET constants, in that order)
    - `MODELS_OPEN` equals `['unet3d', 'retinanet']`
    - `MODELS` (the whatif list) still equals `['cosmoflow', 'resnet50', 'unet3d', 'dlrm', 'retinanet', 'flux']` — unchanged
    - `grep -n "MODELS_OPEN" mlpstorage_py/config.py` returns exactly one line
    - `pytest tests/unit/test_config.py -v` passes (no regressions)
  </acceptance_criteria>

  <done>MODELS_CLOSED = [UNET, RETINANET] and MODELS_OPEN = [UNET, RETINANET] in config.py; test_config.py passes</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite common_args.py — resolve conflict markers, simplify all signatures</name>
  <files>mlpstorage_py/cli/common_args.py</files>

  <read_first>
    mlpstorage_py/cli/common_args.py — read the entire file carefully. Locate:
      - The two conflict blocks (lines ~211-264 and ~357-400 per RESEARCH.md)
      - Current signatures of add_universal_arguments, add_host_arguments,
        add_mpi_arguments, add_dlio_arguments, add_timeseries_arguments
      - The --what-if flag definition (currently in add_universal_arguments, ~line 294)
      - The existing --loops, --allow-invalid-params argument definitions
    .planning/phases/01-core-cli-refactor/01-PATTERNS.md sections:
      "common_args.py" and "Shared Patterns"
  </read_first>

  <action>
    Rewrite mlpstorage_py/cli/common_args.py to resolve both conflict blocks and simplify
    all function signatures. Do not alter the imports section or HELP_MESSAGES dict.

    CONFLICT BLOCK 1 (lines ~211-264, inside add_universal_arguments):
    DELETE the entire conflict block (both <<<<<<< HEAD side and >>>>>>> origin/main side).
    Remove the if req_fileobj: / add_mutually_exclusive_group block for --file/--object (HEAD side).
    Remove the --open / --closed flags mutually-exclusive group (origin/main side).
    Both sides are completely removed.

    SIGNATURE CHANGES — apply to all five functions:

    add_universal_arguments: change signature from
      (parser, req_fileobj, req_results, offer_object, is_closed)
    to:
      (parser, req_results)
    Inside the body, also remove the --loops argument and its if is_closed: gate (lines ~193-203).
    Remove the --allow-invalid-params argument and its if is_closed: gate (lines ~283-292).
    These two arguments move to _add_{cmd}_open_args() in each benchmark builder.
    Rename --what-if to --dry-run: change the add_argument call from
      "--what-if" with dest implicitly what_if
    to:
      "--dry-run" with action="store_true", help="View the configuration that would execute
       and the associated command. Does not execute."
    Keep all other arguments in add_universal_arguments: --config-file, --debug, --verbose,
    --stream-log-level, --verify-lockfile, --skip-validation, --results-dir (req_results controls
    required=), --dry-run.

    add_host_arguments: change signature from (parser, is_closed, required=False) to (parser, required=False).
    Body is unchanged (it never used is_closed).

    add_mpi_arguments: change signature from (parser, is_closed) to (parser).
    Body is unchanged.

    add_dlio_arguments: change signature from (parser, is_closed) to (parser).
    Remove the if is_closed: / else: gate for --params. The --params argument is entirely
    removed from this function — it moves to _add_{cmd}_open_args() in each benchmark builder.
    The parser.set_defaults(params='') call also moves out; callers will set it themselves.
    Keep --dlio-bin-path and any other arguments in the function body.

    add_timeseries_arguments: change signature from (parser, is_closed) to (parser).
    Remove the if is_closed: conditional. Always add all three timeseries args
    (--timeseries-interval, --skip-timeseries, --max-timeseries-samples).
    This function is only called by open/whatif tier builders, so the closed-gating
    is handled by the caller not calling it — not by is_closed inside the function.

    CONFLICT BLOCK 2 (lines ~357-400, between function definitions):
    INSERT add_storage_type_arguments as a standalone function from the origin/main side verbatim:
      def add_storage_type_arguments(parser):
          storage_group = parser.add_argument_group("Storage Type")
          access_proto = storage_group.add_mutually_exclusive_group(required=False)
          access_proto.add_argument(
              "--file",
              action="store_true",
              help="Use POSIX files as the data access method"
          )
          access_proto.add_argument(
              "--object",
              nargs="?",
              type=str,
              const="s3",
              choices=["s3"],
              help=(
                  "Use the given Object API as the data access method, defaults to S3. "
                  "S3 credentials and endpoint are read from environment variables or "
                  "a .env file (AWS_ENDPOINT_URL, AWS_ACCESS_KEY_ID, "
                  "AWS_SECRET_ACCESS_KEY, AWS_REGION, BUCKET, STORAGE_LIBRARY)."
              ),
          )
    Place this function immediately after add_dlio_arguments (or before add_host_arguments —
    the exact position within the module does not matter as long as it is at module scope).
    Change add_host_arguments signature from (parser, is_closed, required=False) to (parser, required=False).
    Change add_mpi_arguments signature from (parser, is_closed) to (parser).

    FINAL CHECK: After making all changes, the file must contain no lines starting with
    "<<<<<<<", "=======", or ">>>>>>>" (conflict markers).
  </action>

  <verify>
    <automated>python3 -c "from mlpstorage_py.cli.common_args import add_universal_arguments, add_host_arguments, add_mpi_arguments, add_dlio_arguments, add_timeseries_arguments, add_storage_type_arguments; print('common_args imports OK')" && python3 -c "import inspect, mlpstorage_py.cli.common_args as m; sig=inspect.signature(m.add_universal_arguments); params=list(sig.parameters); assert params==['parser','req_results'], f'Wrong sig: {params}'; sig2=inspect.signature(m.add_host_arguments); p2=list(sig2.parameters); assert p2==['parser','required'], f'Wrong host sig: {p2}'; print('signatures OK')" && grep -c "<<<<<<\|>>>>>>\|=======" /home/curtis/MLPerfStorage/cli-closed-open/storage/mlpstorage_py/cli/common_args.py 2>/dev/null | grep -q "^0$" && echo "No conflict markers"</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "from mlpstorage_py.cli.common_args import add_universal_arguments"` exits 0
    - `grep "<<<<<<\|>>>>>>" mlpstorage_py/cli/common_args.py` returns no lines
    - `grep "def add_universal_arguments" mlpstorage_py/cli/common_args.py` shows signature `(parser, req_results)`
    - `grep "def add_host_arguments" mlpstorage_py/cli/common_args.py` shows signature `(parser, required=False)`
    - `grep "def add_mpi_arguments" mlpstorage_py/cli/common_args.py` shows signature `(parser):`
    - `grep "def add_dlio_arguments" mlpstorage_py/cli/common_args.py` shows signature `(parser):`
    - `grep "def add_timeseries_arguments" mlpstorage_py/cli/common_args.py` shows signature `(parser):`
    - `grep "def add_storage_type_arguments" mlpstorage_py/cli/common_args.py` returns exactly one line
    - `grep "is_closed" mlpstorage_py/cli/common_args.py` returns no lines
    - `grep "\-\-dry-run" mlpstorage_py/cli/common_args.py` returns a line (--dry-run replaces --what-if)
    - `grep "\-\-what-if" mlpstorage_py/cli/common_args.py` returns no lines
    - `grep "\-\-loops" mlpstorage_py/cli/common_args.py` returns no lines (moved to open builders)
    - `grep "\-\-allow-invalid-params" mlpstorage_py/cli/common_args.py` returns no lines (moved to open builders)
  </acceptance_criteria>

  <done>
    common_args.py imports cleanly; no conflict markers; all five function signatures simplified;
    add_storage_type_arguments present; --dry-run replaces --what-if; is_closed fully removed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → benchmark code | args namespace from argparse passes into benchmark classes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | --allow-invalid-params gate | mitigate | Argument not registered on closed branch (argparse rejects at parse time); no validation bypass path |
| T-01-02 | Information Disclosure | args.dry_run replaces args.what_if | accept | Rename is cosmetic; dry-run is non-destructive read-only mode, no sensitive data |
| T-01-03 | Elevation of Privilege | MODELS_CLOSED with wrong values | mitigate | Correct constant ensures closed submissions cannot use whatif-only models; argparse choices= enforces at parse time |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; pure Python refactor |
</threat_model>

<verification>
After both tasks complete:

1. `python3 -c "from mlpstorage_py.cli.common_args import add_universal_arguments, add_host_arguments, add_mpi_arguments, add_dlio_arguments, add_timeseries_arguments, add_storage_type_arguments"` exits 0
2. `python3 -c "from mlpstorage_py.config import MODELS_CLOSED, MODELS_OPEN"` exits 0
3. `grep -r "<<<<<<<\|>>>>>>>" mlpstorage_py/cli/common_args.py` returns nothing
4. `grep "is_closed" mlpstorage_py/cli/common_args.py` returns nothing
5. `pytest tests/unit/test_config.py tests/unit/test_imports.py -v` passes
</verification>

<success_criteria>
- common_args.py parseable (no SyntaxError on import)
- All five arg-builder function signatures have no is_closed parameter
- add_storage_type_arguments is a standalone function
- --dry-run replaces --what-if in add_universal_arguments
- MODELS_CLOSED = [UNET, RETINANET] and MODELS_OPEN = [UNET, RETINANET] in config.py
- pytest tests/unit/test_config.py passes (no regressions)
</success_criteria>

<output>
Create `.planning/phases/01-core-cli-refactor/01-01-SUMMARY.md` when done
</output>
